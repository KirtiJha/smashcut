---
title: "SVG Line Draw Loader"
description: "An Anime.js-style path drawing loader implemented as a seekable GSAP stroke timeline."
---

import { InstallCommand } from "/snippets/install-command.jsx";

<iframe
  className="w-full aspect-video rounded-xl border-0 bg-zinc-100 dark:bg-zinc-800"
  title="svg-line-draw-loader preview"
  loading="lazy"
  srcDoc={`<!doctype html><html><head><meta charset="utf-8"><style>html,body{margin:0;height:100%;overflow:hidden;background:transparent}smashcut-player{display:block;width:100%;height:100%}</style><script src="https://cdn.jsdelivr.net/npm/@smashcut/player@latest/dist/smashcut-player.global.js"><\/script></head><body><script>fetch("/public/catalog/components/svg-line-draw-loader.json").then(function(r){return r.json()}).then(function(d){var p=document.createElement("smashcut-player");p.setAttribute("srcdoc",d.html);p.setAttribute("controls","");p.setAttribute("autoplay","");p.setAttribute("loop","");p.setAttribute("muted","");p.setAttribute("poster","https://static.heygen.ai/smashcut-oss/docs/images/catalog/components/svg-line-draw-loader.png");document.body.appendChild(p)});<\/script></body></html>`}
/>

## Install

<InstallCommand command="npx smashcut add svg-line-draw-loader" item="svg-line-draw-loader" />

That writes one file: `compositions/components/svg-line-draw-loader.html`.

## Source

<Accordion title={`svg-line-draw-loader.html`}>

```html
<!--
  SVG Line Draw Loader - advanced SmashCut primitive.

  This is intentionally seekable: drive it from a paused GSAP timeline.
  MotionPath examples require GSAP MotionPathPlugin. Three.js examples expose
  a render(progress) adapter instead of a free-running requestAnimationFrame loop.
-->

<div class="sc-advanced-svg-line-draw-loader">
  <svg viewBox="0 0 520 220"><path d="M24 160 C90 28, 188 40, 228 128 S356 234, 494 68" /></svg>
</div>

<style>
  .sc-advanced-svg-line-draw-loader {
    color: var(--fg, #111827);
    font-family: var(--font-body, Inter, system-ui, sans-serif);
  }
  .sc-advanced-svg-line-draw-loader {
    width: 720px;
    min-height: 420px;
    border-radius: var(--radius, 30px);
    padding: var(--space-3, 36px);
    background: var(--surface, #fff);
    box-shadow: 0 28px 80px rgba(15, 23, 42, 0.16);
    display: grid;
    place-items: center;
  }
  .sc-advanced-svg-line-draw-loader .grid {
    display: grid;
    grid-template-columns: repeat(8, 46px);
    gap: var(--space-1, 10px);
  }
  .sc-advanced-svg-line-draw-loader .cell {
    width: 46px;
    height: 46px;
    border-radius: 14px;
    background: color-mix(in srgb, var(--fg, #111827) 45%, transparent);
    transform: translateY(var(--sc-cell-y, 0px)) scale(var(--sc-cell-scale, 1));
    opacity: var(--sc-cell-opacity, 1);
  }
  .sc-advanced-svg-line-draw-loader svg {
    width: 520px;
    height: 220px;
    overflow: visible;
  }
  .sc-advanced-svg-line-draw-loader path {
    fill: none;
    stroke: var(--accent, #18181b);
    stroke-width: 14;
    stroke-linecap: round;
    stroke-dasharray: 900;
    stroke-dashoffset: var(--sc-dash, 0);
  }
  .sc-advanced-svg-line-draw-loader .stack {
    position: relative;
    width: 520px;
    height: 260px;
  }
  .sc-advanced-svg-line-draw-loader .card {
    position: absolute;
    inset: 0;
    border-radius: 26px;
    background: linear-gradient(135deg, var(--surface, #fafafa), var(--surface, #e4e4e7));
    border: 1px solid var(--border, rgba(15, 23, 42, 0.08));
    box-shadow: 0 20px 54px rgba(15, 23, 42, 0.14);
    transform: translateY(var(--sc-card-y, 0px)) scale(var(--sc-card-scale, 1));
    opacity: var(--sc-card-opacity, 1);
  }
</style>

<!--
  Timeline integration:
  const startTime = 0;
  tl.fromTo('.sc-advanced-svg-line-draw-loader path', { '--sc-dash': 900 }, { '--sc-dash': 0, duration: 1.35, ease: 'power3.inOut' }, startTime);
-->
```

</Accordion>

## Usage

Open `compositions/components/svg-line-draw-loader.html` and paste its contents into your composition. The comment header describes the timeline integration and any optional dependency setup.

{/* hf:generated-footer */}

Tagged `video-primitive` `advanced-motion` `anime-inspired` `smashcut-native`.

## Related topics

- [Browse the complete Catalog](/catalog)
- [Add assets and Catalog items in Studio](/studio/assets-and-blocks)
- [Build a richer composition](/go-further)
