---
title: "@smashcut/parsers"
description: "The GSAP + HTML parser/writer suite — standalone, zero @smashcut/* dependencies."
---

The parsers package owns the GSAP animation parser/writer, HTML composition
parser, sc-id stamping, and spring-ease generation. It has no
`@smashcut/*` runtime dependencies, so it can be used without the rest of
the framework.

```bash
npm install @smashcut/parsers
```

## When to Use

<Tip>
  **Most users do not need to install `@smashcut/parsers` directly.** [`@smashcut/core`](/packages/core) re-exports the parser API it needs, and the [CLI](/packages/cli) / [studio](/packages/studio) depend on it transitively. Reach for it directly only when you want the parsing layer **without** pulling in the rest of core.
</Tip>

**Use `@smashcut/parsers` when you need to:**
- Parse HTML compositions into structured TypeScript objects
- Parse, edit, and re-serialize GSAP timeline scripts (AST round-trip)
- Stamp deterministic `sc-id` attributes onto a document
- Build tooling that touches the parsing layer but doesn't need core's runtime, compiler, or generators

## Package Exports

| Import | Description |
|--------|-------------|
| `@smashcut/parsers` | HTML parser, GSAP serialize/validate helpers, sc-ids, shared types |
| `@smashcut/parsers/gsap-parser` | GSAP parser exports, `parseGsapScript`, `SUPPORTED_PROPS`, `SUPPORTED_EASES` |
| `@smashcut/parsers/gsap-parser-acorn` | Acorn-based GSAP parser (browser-safe, read path) |
| `@smashcut/parsers/gsap-writer-acorn` | Acorn-based GSAP writer (mutation helpers) |
| `@smashcut/parsers/gsap-parser-recast` | Recast-based GSAP parser/writer (legacy implementation) |
| `@smashcut/parsers/gsap-constants` | `SUPPORTED_PROPS`, `SUPPORTED_EASES`, property groups |
| `@smashcut/parsers/spring-ease` | Spring-ease curve generation |
| `@smashcut/parsers/sc-ids` | Deterministic element id stamping |
| `@smashcut/parsers/slideshow` | Slideshow manifest parser (`parseSlideshowManifest`, `resolveSlideshow`) |
| `@smashcut/parsers/composition` | Pure, browser-safe composition primitives (data types, font aliases, URL helper) |
| `@smashcut/parsers/asset-paths` | Node-only asset-path rewriting helpers (`rewriteAssetPath`, …) |
| `@smashcut/parsers/sub-composition-validity` | Sub-composition validation utilities |

<Info>
  The package ships focused subpath entries. Import
  `@smashcut/parsers/sc-ids` when you do not need the GSAP AST machinery.
</Info>

## HTML Parsing

Round-trip between HTML and structured data:

```typescript
import {
  parseHtml,
  extractCompositionMetadata,
  validateCompositionHtml,
} from '@smashcut/parsers';
import type { ParsedHtml, CompositionMetadata } from '@smashcut/parsers';

// Parse HTML into structured data
const parsed: ParsedHtml = parseHtml(htmlString);
// parsed.elements, parsed.gsapScript, parsed.styles, parsed.resolution, parsed.keyframes

// Extract composition metadata (id, duration, dimensions, variables)
const meta: CompositionMetadata = extractCompositionMetadata(htmlString);

// Validate HTML structure
const result = validateCompositionHtml(htmlString);
// result.valid, result.errors
```

### Modifying HTML

```typescript
import {
  updateElementInHtml,
  addElementToHtml,
  removeElementFromHtml,
} from '@smashcut/parsers';

const updated = updateElementInHtml(html, 'el-1', { start: 5 });
const added = addElementToHtml(html, newElement);
const cleaned = removeElementFromHtml(html, 'el-1');
```

## GSAP Script Parsing

The acorn parser/writer is the primary path (browser-safe). Parse a timeline script, mutate it, and serialize it back without losing surrounding code:

```typescript
import { parseGsapScriptAcorn, extractGsapLabels } from '@smashcut/parsers/gsap-parser-acorn';
import {
  updateAnimationInScript,
  addAnimationToScript,
  removeAnimationFromScript,
  updateKeyframeInScript,
  addKeyframeToScript,
  shiftPositionsInScript,
  scalePositionsInScript,
} from '@smashcut/parsers/gsap-writer-acorn';
import type { GsapAnimation, GsapMethod, ParsedGsap } from '@smashcut/parsers';

const parsed: ParsedGsap = parseGsapScriptAcorn(scriptContent);
// parsed.animations, parsed.timelineVar, parsed.preamble, parsed.postamble

// Mutation helpers operate on the script text and preserve unrelated code
const next = updateAnimationInScript(scriptContent, animationId, { duration: 2 });
```

High-level serialize / validate / keyframe-conversion helpers are on the main entry:

```typescript
import {
  serializeGsapAnimations,
  getAnimationsForElementId,
  validateCompositionGsap,
  keyframesToGsapAnimations,
  gsapAnimationsToKeyframes,
} from '@smashcut/parsers';
```

### GSAP Constants

```typescript
import {
  SUPPORTED_PROPS,    // animatable properties
  SUPPORTED_EASES,    // available easing functions
  PROPERTY_GROUPS,
} from '@smashcut/parsers/gsap-constants';
import type { PropertyGroupName } from '@smashcut/parsers/gsap-constants';
```

## sc-ids

Deterministic element identity for stable diffing and editing:

```typescript
import { ensureHfIds, mintHfId } from '@smashcut/parsers/sc-ids';

// Stamp sc-id attributes onto every editable element in a document
const withIds = ensureHfIds(htmlString);
```

## Spring Ease

```typescript
import { generateSpringEaseData, SPRING_PRESETS } from '@smashcut/parsers/spring-ease';
```

## Related topics

<CardGroup cols={2}>
  <Card title="@smashcut/core" icon="cube" href="/packages/core">
    Types, generators, runtime, and compiler — re-exports the parser API it needs.
  </Card>
  <Card title="@smashcut/lint" icon="circle-check" href="/packages/lint">
    The composition linter, built on the parsers.
  </Card>
  <Card title="@smashcut/studio-server" icon="server" href="/packages/studio-server">
    The studio preview server, built on the parsers.
  </Card>
  <Card title="CLI" icon="terminal" href="/packages/cli">
    Create, preview, lint, and render compositions.
  </Card>
</CardGroup>
